# Наименование квеста

## Что сделано

Короткое описание того, что сделано в рамках квеста

### API

Перечень реализованных эндпоинтов:

- **`<METHOD> <PATH>`**
  - **Query/Path params**: `<...>`
  - **Код 200**: `что возвращаем`
  - **Код 400**: `что возвращаем`
  - **Код 404**: `что возвращаем`

- **`<METHOD> <PATH>`**
  - **Query/Path params**: `<...>`
  - **Код 200**: `что возвращаем`
  - **Код 400**: `что возвращаем`
  - **Код 404**: `что возвращаем`

## Запуск

```bash
make build up migrate
```
По адресу `localhost:8000/docs` можно найти документацию API-эндпоинтов

## Автотесты

```bash
make test
```

- `<tests/...::test_name>` — `что проверяет`
- `<tests/...::test_name>` — `что проверяет`
- `<tests/...::test_name>` — `что проверяет`

Тесты успешно проходят (см. джобу tests)

## ADR

Если требуется

- **Альтернативы**:
  - `<вариант 1>`
  - `<вариант 2>`
  - `<вариант 3>`
- **Выбор**: `<что выбрали и почему>`

## Файлы

### Middleware

### API эндпоинты

### Сервисы

### CRUD

### Схемы

### Автотесты